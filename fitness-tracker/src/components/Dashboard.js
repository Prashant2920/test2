import React, { useEffect } from 'react';

const Dashboard = () => {
    useEffect(() => {
        // Fetch user data and activities here
    }, []);

    return (
        <div>
            <h1>Dashboard</h1>
            {/* Display activities and stats here */}
        </div>
    );
};

export default Dashboard;
